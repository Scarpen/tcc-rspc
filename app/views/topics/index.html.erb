<h1> Forum </h1>

<% @topics.each do |p| %>
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title"><%= p.topic_title%></h3>
  </div>
  <div class="panel-body">
    <%= p.description %>
  </div>
  <div class="panel-body">
  	<%= link_to "Editar", edit_topic_path(id_topic: p.id) %>
  	<br>
  	<%= link_to "Deletar", topic_path(id_topic: p.id), method: :delete, data: { confirm: 'Are you sure?'} %>
  </div>
</div>

<% end %>


<br>
<br>

<%= link_to "Criar novo Topico",create_topic_path %>