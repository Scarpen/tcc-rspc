<% @assist = assist %>
<%= simple_form_for @assist, :url => quit_request_tasks_path(:id_assist => @assist) do |f| %>
  <% if @assist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assist.errors.count, "error") %> prohibited this assist from being saved:</h2>

      <ul>
      <% @assist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :reason %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>