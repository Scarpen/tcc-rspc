<li class="list-group-item">
  <span class="glyphicon glyphicon-edit"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.name : 'Guest' %></strong>,
  <% if a.trackable %>
     your request for enter in the project <%= link_to a.trackable.project.name, project_path(a.trackable.project) %> was accepted.
  <% else %>
    was unfollowing the project.
  <% end %>
</li>

