<li class="list-group-item">

   	<small style="padding-left:45px; margin-top:-10px" class="text-muted"><%= a.created_at.strftime("%d/%m/%Y %H:%M:%S") %></small>

  	<br/>

	<div style="width: 40x; border: 1px solid #DDD; height: 40px;line-height:38px;float:left; margin-left: 0px; margin-top: -16px; margin-right: 5px;">
		<%= image_tag(a.trackable.project.avatar, class: "notification") %>
	</div>

  
  <% if a.trackable %>
     Sua solicitação para entrar no projeto <strong><%= link_to a.trackable.project.name, project_path(a.trackable.project), class: "user_link" %></strong> foi aceita. <%= welcome_project(a.trackable.user) %>
  <% else %>
    was unfollowing the project.
  <% end %>
  <div class="clearfix"></div>
</li>

