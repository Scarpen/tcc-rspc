<li class="list-group-item">
  <span class="glyphicon glyphicon-plus"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong>New Project</strong><br>
  <% if a.trackable %>
    <strong><%= User.find(a.trackable.creator_id).name %></strong> added the new project <%= link_to a.trackable.name, project_path(a.trackable) %>.
  <% else %>
    added the story that is currently deleted.
  <% end %>
</li>