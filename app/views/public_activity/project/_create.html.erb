<li class="list-group-item">


  <small style="padding-left:45px; margin-top:-10px" class="text-muted"><%= a.created_at.strftime("%d/%m/%Y %H:%M:%S") %></small>

    <br/>

  <div style="width: 40x; border: 1px solid #DDD; height: 40px;line-height:38px;float:left; margin-left: 0px; margin-top: -16px; margin-right: 5px;">
      <%= image_tag(a.trackable.avatar, class: "notification") %>
  </div>

  <% if a.trackable %>
    <%= User.find(a.trackable.creator_id).name %> criou o projeto <strong><%= link_to a.trackable.name, project_path(a.trackable), class: "user_link" %></strong> na sua área de interesse de <%= interest_project(a.trackable) %>.
  <% end %>
  <div class="clearfix"></div>
</li>