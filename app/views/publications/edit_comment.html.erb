<%= simple_form_for @comment, :url => update_comment_publications_path(:id => @comment.id) do |f| %>

<%= f.input :description%>
<%= f.input :publication_id, :as => :hidden, :input_html => {:value =>  @publication} %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>